O prefixo findBy é utilizado para gerar consultas baseadas em atributos da entidade.

Exemplos:
Pessoa findByCodigo(Long codigo);               <- Retorna uma pessoa.
Iterable<Pessoa> findByCidade(String cidade);   <- Retorna uma coleção.
List<Pessoa> findByNome(String nome);           <- Retorna uma lista.

-----------------------------------

Um método padrão que usamos é o findById, que retorna um objeto a partir do código.