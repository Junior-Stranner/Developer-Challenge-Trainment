O prefixo findBy é utilizado para gerar consultas baseadas em atributos da entidade.

Exemplos:
PessoaModelo findByCodigo(Long codigo);               <- Retorna uma pessoa.
Iterable<PessoaModelo> findByCidade(String cidade);   <- Retorna uma coleção.
List<PessoaModelo> findByNome(String nome);           <- Retorna uma lista.

-----------------------------------

Um método padrão que usamos é o findById, que retorna um objeto a partir do código.